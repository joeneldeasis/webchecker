{
  "name": "webstat",
  "version": "1.0.0",
  "description": "Tool for checking the website if it is up or down",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "is-reachable": "^2.3.2",
    "request": "^2.81.0"
  }
}
